"use client";
import {useState} from "react";
import {ArrowRight,CheckCircle2,Clock3,FileText,GitBranch,Plus,ShieldCheck,Sparkles,Upload,AlertTriangle} from "lucide-react";
const initial=[{title:"Family document renewal",status:"Active",progress:68,items:"4 documents · 2 actions",risk:"2 missing items"}];
export default function Home(){const [situations,setSituations]=useState(initial);const [show,setShow]=useState(false);const [name,setName]=useState("");const add=()=>{if(!name.trim())return;setSituations([{title:name,status:"Active",progress:8,items:"0 documents · 0 actions",risk:"Needs information"},...situations]);setName("");setShow(false)};return <main><header><div className="brand"><div className="logo">C</div><div><b>CueMesh</b><span>Situation intelligence</span></div></div><button className="ghost"><ShieldCheck size={16}/> Human-controlled</button></header><section className="hero"><div><div className="eyebrow"><Sparkles size={15}/> UNDERSTAND THE WHOLE SITUATION</div><h1>From scattered information<br/><em>to clear next steps.</em></h1><p>CueMesh connects documents, people, deadlines and actions into one evidence-backed situation. AI proposes. You decide.</p><button className="primary" onClick={()=>setShow(true)}><Plus size={18}/> New situation <ArrowRight size={17}/></button></div><div className="heroCard"><div className="miniTitle">Situation health</div><div className="score">72<span>/100</span></div><div className="bar"><i style={{width:"72%"}}/></div><p>3 important pieces connected</p><div className="chips"><span>✓ Evidence linked</span><span>! 2 gaps</span></div></div></section><section className="content"><div className="sectionHead"><div><h2>Your situations</h2><p>Everything important, in context.</p></div><button className="secondary" onClick={()=>setShow(true)}><Plus size={16}/> Create</button></div><div className="grid">{situations.map((s,i)=><article className="card" key={i}><div className="cardTop"><span className="status"><span/> {s.status}</span><span className="muted">Today</span></div><h3>{s.title}</h3><div className="progress"><div><span>Situation completeness</span><b>{s.progress}%</b></div><div className="bar"><i style={{width:s.progress+"%"}}/></div><div className="meta"><span><FileText size={15}/>{s.items}</span><span className="warning"><AlertTriangle size={15}/>{s.risk}</span></div><button className="open">Open situation <ArrowRight size={16}/></button></article>)}<article className="empty" onClick={()=>setShow(true)}><div><Plus/></div><b>Start a new situation</b><span>Family, work, travel, documents, anything important.</span></article></div></section><section className="features"><div><GitBranch/><b>Life graph</b><span>See how people, documents, events and actions connect.</span></div><div><AlertTriangle/><b>Gap detection</b><span>Find what is missing before it becomes a problem.</span></div><div><CheckCircle2/><b>Human approval</b><span>Nothing important executes without your approval.</span></div><div><Clock3/><b>Follow-up</b><span>Keep track after the immediate task is done.</span></div></section>{show&&<div className="modal"><div className="dialog"><button className="close" onClick={()=>setShow(false)}>×</button><div className="eyebrow"><Sparkles size={14}/> NEW SITUATION</div><h2>What are you dealing with?</h2><p>Give the situation a simple name. You can add documents and details later.</p><input autoFocus value={name} onChange={e=>setName(e.target.value)} onKeyDown={e=>e.key==="Enter"&&add()} placeholder="e.g. Home loan application"/><button className="primary full" onClick={add}>Create situation <ArrowRight size={17}/></button></div></div>}</main>}