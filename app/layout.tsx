import "./globals.css";
export const metadata={title:"CueMesh — Situation Intelligence",description:"Turn scattered life information into clear, evidence-backed next steps."};
export default function RootLayout({children}:{children:React.ReactNode}){return <html lang="en"><body>{children}</body></html>}